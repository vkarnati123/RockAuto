
  # RockAuto

  This is a code bundle for RockAuto. The original project is available at https://www.figma.com/design/O53CE1TNJJtaihe2olv39A/RockAuto.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  